class FizzBuzz {
    constructor(name) {
        this.name = name;
    }

    fizzBuzz(data) {
        for(let i = 1; i <= data; i++) {
            const data1 = i % 3 === 0 && i % 5 === 0;
        }
    }
}